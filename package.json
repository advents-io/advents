{
  "name": "advents",
  "scripts": {
    "dev": "pnpm --dir apps/app run dev",
    "app": "pnpm --dir apps/app run",
    "db": "pnpm --dir packages/db run",
    "doc": "pnpm --dir modules/docs run",
    "sdk": "pnpm --dir modules/advents-react-native run",
    "lint": "eslint . --fix && prettier . --write",
    "clear": "./scripts/clear.sh"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.5.3"
  },
  "eslintConfig": {
    "extends": [
      "./packages/eslint-config/node.js"
    ]
  },
  "prettier": "./packages/eslint-config/prettier.config.js"
}
