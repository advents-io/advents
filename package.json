{
  "name": "advents",
  "scripts": {
    "dev": "pnpm --dir apps/app dev",
    "app": "pnpm --dir apps/app",
    "db": "pnpm --dir packages/db",
    "doc": "pnpm --dir apps/docs",
    "sdk": "pnpm --dir apps/react-native",
    "lint": "eslint . --fix && prettier . --write",
    "clear": "./scripts/clear.sh"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.5.3"
  },
  "eslintConfig": {
    "extends": [
      "./packages/eslint-config/node.js"
    ]
  },
  "prettier": "./packages/eslint-config/prettier.config.js"
}
